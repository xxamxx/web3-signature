{
  "name": "web3-signature",
  "version": "1.0.0-beta.1",
  "description": "Web3 Signature unified verify package",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist/* && tsc",
    "test": "jest --require=ts-node/register --require=tsconfig-paths/register --",
    "prepublish": "npm run test && npm run build"
  },
  "keywords": [
    "web3",
    "wallet",
    "signature",
    "verify",
    "verification"
  ],
  "author": "xxamxx",
  "license": "ISC",
  "dependencies": {
    "ethers": "^6.7.1"
  },
  "files": [
    "dist/**/*"
  ],
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-loader": "^9.4.4",
    "typescript": "^5.2.2"
  }
}
